# Railway database connection details (Railway)
# WARNING: This file contains secrets. Remove before committing to a public repo.

# Original MYSQL_URL provided by Railway
mysql.url=mysql://root:LwUxstCGVVFEQqZiJIUKsiuBSBlCCKsY@MySQL.railway.internal:3306/railway

# JDBC form used by Spring Boot (preferred)
jdbc.url=jdbc:mysql://MySQL.railway.internal:3306/railway?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# Database credentials
db.username=root
db.password=LwUxstCGVVFEQqZiJIUKsiuBSBlCCKsY
db.port=3306

# Spring-compatible names (if you want Spring to read these directly, copy values into environment or application.properties)
spring.datasource.url=${JDBC_DATABASE_URL:${jdbc.url}}
spring.datasource.username=${DB_USERNAME:${db.username}}
spring.datasource.password=${DB_PASSWORD:${db.password}}
